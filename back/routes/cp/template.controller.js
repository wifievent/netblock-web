const path = require('path');
const { Page, User } = require(path.resolve(__dirname, '..', '..', 'models'));
const logger = require('../../config/winston');

const read = async (req, res, next) => {

}

const render = async (req, res, next) => {

}

module.exports = {
  read,
  render
}